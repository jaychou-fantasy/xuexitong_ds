{
  "mcpServers": {
    "chrome-devtools": {
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp"],
      "description": "Google Chrome DevTools MCP — browser DOM inspection, iframe analysis, console access, network monitoring"
    }
  }
}
